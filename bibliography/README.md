# Publications

Edit `publications.bib` to update the Publications page.

To attach a locally hosted paper:

1. Put the PDF in `files/publications/`.
2. Set the entry's `pdf` field to the exact PDF filename.
3. Run `python3 scripts/build_publications.py`.

The repository's pre-commit hook also runs the converter automatically when
committing through VS Code or the command line.
